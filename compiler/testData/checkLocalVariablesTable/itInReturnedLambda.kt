fun foo() {
    fun bar() : (Int) -> Unit {
        return {
            1
        }
    }
}

// METHOD : ItInReturnedLambda$foo$1$1.invoke(I)V
// VARIABLE : NAME=this TYPE=LItInReturnedLambda$foo$1$1; INDEX=0
// VARIABLE : NAME=it TYPE=I INDEX=1
